{% extends 'base.html.twig' %}

{% block main %}
    <div class="container body-content">
        <div class="row">
            {% for articlee in articlees %}
                <div class="col-md-6">
                    <article>
                        <header>
                            <h2>{{ articlee.title }}</h2>
                        </header>

                        <p>
                            {{ articlee.summary }}
                        </p>

                        <small class="author">
                            {{ articlee.author }}
                        </small>

                        <footer>
                            <div class="pull-right">
                                <a class="btn btn-default btn-xs"
                                   href="{{ path('articlee_view', {'id': articlee.id}) }}">Read more &raquo;</a>
                            </div>
                        </footer>
                    </article>
                </div>
            {% endfor %}
        </div>
    </div>


{% endblock %}
