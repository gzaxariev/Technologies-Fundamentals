function multiplayDividNumbers(args) {
    let n = Number(args[0]);
    let x = Number(args[1]);

    if(n > x){
        console.log(n / x);
        return;
    }
    if(x => n){
        console.log(n * x);
        return;
    }

}
multiplayDividNumbers([
    '144',
    '12'

]);
